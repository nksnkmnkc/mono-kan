<table class="table">
  <tr><%= image_tag teacher.get_profile_image(100,100), size:'100x100' %></tr>
  <tr>
	  <th>名前</th>
	  <th><%= teacher.name %></th>
  </tr>
</table>

  <% if teacher.name != "ゲスト" %>
  <div class="row">
    <%= link_to "編集する", teachers_information_edit_path(teacher), class:"btn btn-success" %>
  </div>
  <% end %>