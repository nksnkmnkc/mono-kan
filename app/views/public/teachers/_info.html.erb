<table class="table">
  <tr><%= image_tag teacher.get_profile_image(100,100) %></tr>
  <tr>
	  <th>名前</th>
	  <th><%= teacher.name %></th>
  </tr>
</table>

<!--ログインティーチャーの詳細ページのみ編集するボタンを表示-->
  <% if teacher.name != "ゲスト" %>
  <div class="row">
    <%= link_to "編集する", teachers_information_edit_path(teacher), class:"btn btn-success" %>
  </div>
  <% end %>